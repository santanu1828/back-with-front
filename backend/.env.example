# Google Gemini API Key (Required for live LLM responses)
GOOGLE_API_KEY=your_gemini_api_key_here

# MongoDB Connection URI (Defaults to localhost:27017, fallback to in-memory if offline)
MONGODB_URI=mongodb://localhost:27017

# Gemini Model Name
MODEL_NAME=gemini-2.5-flash
